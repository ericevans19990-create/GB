# Copy this file to `.env.local` and replace the values with your own.
# Example for Formspree (replace <id> with your form id):
VITE_FORM_ENDPOINT=https://formspree.io/f/<id>
VITE_FORM_ENCODE=form

# Other example env vars used by the project:
# VITE_API_URL=https://api.example.com
# VITE_SENTRY_DSN=

# SMTP (PHPMailer) example - used by public/api/form_smtp.php
# If you want to send via SMTP set these values and use the SMTP handler URL
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=587
SMTP_USER=your@domain.com
SMTP_PASS=supersecret
SMTP_FROM=your@domain.com
SMTP_FROM_NAME="GLORYS Business"
SMTP_SECURE=tls # or ssl
SITE_OWNER=contact@your-domain.com
